<Window
    x:Class="MyShop.Views.Windows.LoginWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="MyShop - Login">

    <Grid DataContext="{x:Bind ViewModel}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#FF8A2BE2" Offset="0.0" />
                    <GradientStop Color="#FF6A5ACD" Offset="0.4" />
                    <GradientStop Color="#FFB59AE8" Offset="1.0" />
                </LinearGradientBrush>
            </Grid.Background>
            <StackPanel HorizontalAlignment="Left" VerticalAlignment="Center" Margin="48">
                <TextBlock FontSize="56" FontWeight="Bold" Foreground="White" Text="Welcome" />
                <TextBlock FontSize="56" FontWeight="Bold" Foreground="White" Text="Back!" Margin="0,8,0,0" />
            </StackPanel>
        </Grid>

        <Grid Grid.Column="1" Padding="48">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Top">
                <TextBlock FontSize="28" FontWeight="SemiBold" Foreground="#333333" Text="Login" />
                <TextBlock FontSize="12" Foreground="#777777" Text="Welcome back! Please login to your account." Margin="0,8,0,0" />
            </StackPanel>

            <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Stretch" Width="420">
                <TextBlock FontSize="12" Foreground="#666666" Text="User Name" Margin="0,0,0,6" />
                <TextBox
                    x:Name="UsernameTextBox"
                    PlaceholderText="username@gmail.com"
                    Text="{Binding Username, Mode=TwoWay}"
                    Margin="0,0,0,12" />

                <TextBlock FontSize="12" Foreground="#666666" Text="Password" Margin="0,6,0,6" />
                <PasswordBox
                    x:Name="PasswordBox"
                    PasswordChanged="PasswordBox_PasswordChanged"
                    PlaceholderText="Enter your password"
                    Margin="0,0,0,12" />

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="0,0,0,12">
                    <CheckBox Content="Remember Me" IsChecked="{Binding IsRememberMe, Mode=TwoWay}" Foreground="#444444" />
                </StackPanel>

                <Button
                    Content="Login"
                    Command="{Binding LoginCommand}"
                    HorizontalAlignment="Stretch"
                    Background="#7B61FF"
                    Foreground="White"
                    FontWeight="SemiBold"
                    Height="44"
                    Margin="0,8,0,12" />

                <TextBlock
                    x:Name="ErrorTextBlock"
                    Foreground="Red"
                    Text="{Binding ErrorMessage}"
                    TextWrapping="Wrap" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>

