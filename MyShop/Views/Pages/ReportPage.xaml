<Page
    x:Class="MyShop.Views.Pages.ReportPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid RowSpacing="0" x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Padding="20" Background="{ThemeResource SystemChromeLowColor}">
            <TextBlock Text="Revenue Report" Style="{StaticResource HeaderTextBlockStyle}" Margin="0,0,0,10"/>
            <TextBlock Text="Analysis of sales and revenue data" Style="{StaticResource SubtitleTextBlockStyle}" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"/>
            
            <!-- Controls Row 1 -->
            <Grid ColumnSpacing="15" Margin="0,15,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="From:" VerticalAlignment="Center"/>
                <CalendarDatePicker Grid.Column="1" x:Name="FromDatePicker" PlaceholderText="Start date" DateChanged="OnDateRangeChanged"/>
                <TextBlock Grid.Column="2" Text="To:" VerticalAlignment="Center"/>
                <CalendarDatePicker Grid.Column="3" x:Name="ToDatePicker" PlaceholderText="End date" DateChanged="OnDateRangeChanged"/>
                <TextBlock Grid.Column="4" Text="Period:" VerticalAlignment="Center" Margin="20,0,10,0"/>
                <ComboBox Grid.Column="5" x:Name="TimePeriodCombo" SelectionChanged="OnTimePeriodChanged" Width="120">
                    <ComboBoxItem Content="Day" Tag="Day" IsSelected="True"/>
                    <ComboBoxItem Content="Week" Tag="Week"/>
                    <ComboBoxItem Content="Month" Tag="Month"/>
                    <ComboBoxItem Content="Year" Tag="Year"/>
                </ComboBox>
            </Grid>
        </StackPanel>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Padding="20" Spacing="20">
                
                <!-- Summary Cards -->
                <StackPanel Spacing="15">
                    <TextBlock Text="Summary" Style="{StaticResource SubheaderTextBlockStyle}"/>
                    
                    <Grid ColumnSpacing="15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Padding="20" CornerRadius="8" Background="{ThemeResource LayerFillColorDefaultBrush}" BorderThickness="1" BorderBrush="{ThemeResource ControlElevationBorderBrush}">
                            <StackPanel Spacing="8">
                                <TextBlock Text="Total Revenue" FontSize="12" Foreground="{ThemeResource TextFillColorSecondary}"/>
                                <TextBlock x:Name="TotalRevenueText" Text="0 ₫" FontSize="24" FontWeight="Bold" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="1" Padding="20" CornerRadius="8" Background="{ThemeResource LayerFillColorDefaultBrush}" BorderThickness="1" BorderBrush="{ThemeResource ControlElevationBorderBrush}">
                            <StackPanel Spacing="8">
                                <TextBlock Text="Total Orders" FontSize="12" Foreground="{ThemeResource TextFillColorSecondary}"/>
                                <TextBlock x:Name="TotalOrdersText" Text="0" FontSize="24" FontWeight="Bold" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="2" Padding="20" CornerRadius="8" Background="{ThemeResource LayerFillColorDefaultBrush}" BorderThickness="1" BorderBrush="{ThemeResource ControlElevationBorderBrush}">
                            <StackPanel Spacing="8">
                                <TextBlock Text="Average Order Value" FontSize="12" Foreground="{ThemeResource TextFillColorSecondary}"/>
                                <TextBlock x:Name="AverageOrderValueText" Text="0 ₫" FontSize="24" FontWeight="Bold" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>

                <!-- Revenue Chart -->
                <StackPanel Spacing="15">
                    <TextBlock Text="Total Revenue by Period" Style="{StaticResource SubheaderTextBlockStyle}"/>
                    <Border Padding="20" CornerRadius="8" Background="{ThemeResource LayerFillColorDefaultBrush}" BorderThickness="1" BorderBrush="{ThemeResource ControlElevationBorderBrush}" Height="350">
                        <Grid>
                            <Canvas x:Name="RevenueChartCanvas" Background="Transparent"/>
                            <TextBlock x:Name="NoDataMessageRevenue" Text="No data available" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondary}"/>
                        </Grid>
                    </Border>
                </StackPanel>

                <!-- Product Quantity Chart -->
                <StackPanel Spacing="15">
                    <Grid ColumnSpacing="15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Column="0" Text="Total Product Sold Quantity by Period" Style="{StaticResource SubheaderTextBlockStyle}"/>
                        
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                            <TextBlock Text="Filter by Product:" VerticalAlignment="Center" FontSize="12"/>
                            <ComboBox x:Name="ProductCombo" SelectionChanged="OnProductSelected" PlaceholderText="All Products" Width="250">
                                <ComboBoxItem Content="All Products" Tag="-1" IsSelected="True"/>
                            </ComboBox>
                        </StackPanel>
                    </Grid>
                    
                    <Border Padding="20" CornerRadius="8" Background="{ThemeResource LayerFillColorDefaultBrush}" BorderThickness="1" BorderBrush="{ThemeResource ControlElevationBorderBrush}" Height="350">
                        <Grid>
                            <Canvas x:Name="QuantityChartCanvas" Background="Transparent"/>
                            <TextBlock x:Name="NoDataMessageQuantity" Text="No data available" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondary}"/>
                        </Grid>
                    </Border>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
