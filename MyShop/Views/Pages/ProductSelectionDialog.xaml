<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="MyShop.Views.Pages.ProductSelectionDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyShop.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d"
    Title="Select Products for Order"
    PrimaryButtonText="Create Order"
    CloseButtonText="Cancel">

    <Grid Padding="20,20,20,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Search Bar -->
        <TextBox 
            x:Name="SearchBox"
            Grid.Row="0"
            PlaceholderText="Search products by name or SKU..."
            Margin="0,0,0,15"
            TextChanged="OnSearchTextChanged"/>

        <!-- Products DataGrid -->
        <controls:DataGrid
            x:Name="ProductsDataGrid"
            Grid.Row="1"
            AutoGenerateColumns="False"
            CanUserReorderColumns="False"
            CanUserResizeColumns="True"
            GridLinesVisibility="All"
            BorderThickness="1"
            BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}"
            HorizontalScrollBarVisibility="Auto">

            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn
                    Header="ID"
                    Binding="{Binding Product.ProductId}"
                    Width="50"
                    IsReadOnly="True"/>

                <controls:DataGridTextColumn
                    Header="SKU"
                    Binding="{Binding Product.Sku}"
                    Width="100"
                    IsReadOnly="True"/>

                <controls:DataGridTextColumn
                    Header="Product Name"
                    Binding="{Binding Product.Name}"
                    Width="250"
                    IsReadOnly="True"/>

                <controls:DataGridTextColumn
                    Header="Unit Price (₫)"
                    Binding="{Binding Product.ImportPrice}"
                    Width="160"
                    IsReadOnly="True"/>

                <controls:DataGridTextColumn
                    Header="Available"
                    Binding="{Binding Product.Count}"
                    Width="100"
                    IsReadOnly="True"/>

                <controls:DataGridTemplateColumn
                    Header="Quantity"
                    Width="120">
                    <controls:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <NumberBox
                                x:Name="QuantityBox"
                                Value="{Binding Quantity, Mode=TwoWay}"
                                Minimum="0"
                                Maximum="{Binding Product.Count}"
                                SpinButtonPlacementMode="Hidden"
                                SmallChange="1"
                                ValueChanged="OnQuantityValueChanged"/>
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellTemplate>
                </controls:DataGridTemplateColumn>

                <controls:DataGridTextColumn
                    Header="Total (₫)"
                    Binding="{Binding TotalPrice, UpdateSourceTrigger=PropertyChanged}"
                    Width="180"
                    IsReadOnly="True"/>
            </controls:DataGrid.Columns>
        </controls:DataGrid>

        <!-- Summary -->
        <Border 
            Grid.Row="2"
            Padding="15"
            Margin="0,15,0,0"
            Background="{ThemeResource LayerFillColorDefaultBrush}"
            BorderThickness="1"
            BorderBrush="{ThemeResource ControlElevationBorderBrush}"
            CornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock 
                    Grid.Column="0"
                    Text="Select products and set quantities above, then click 'Create Order'" 
                    Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                    FontSize="13"
                    VerticalAlignment="Center"/>

                <StackPanel 
                    Grid.Column="1"
                    Orientation="Horizontal" 
                    Spacing="30"
                    VerticalAlignment="Center"
                    Margin="30,0,0,0">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="Selected Items:" FontWeight="Bold"/>
                        <TextBlock x:Name="SelectedCountText" Text="0" FontWeight="SemiBold"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel 
                    Grid.Column="2"
                    Orientation="Horizontal" 
                    Spacing="10"
                    VerticalAlignment="Center"
                    Margin="30,0,0,0">
                    <TextBlock Text="Total Price:" FontWeight="Bold"/>
                    <TextBlock x:Name="TotalPriceText" Text="0 ₫" FontWeight="SemiBold" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</ContentDialog>